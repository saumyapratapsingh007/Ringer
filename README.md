# Ringer
A native Android utility app built in Java that automates Ringer Mode transitions. Implements Foreground Services for persistent background execution to revert silent mode after user-defined intervals.
